#include "main.h"

/**
 * _strlen - Entry point
 * Description: Returns the lenght of a string
 * @s: Type char pointer
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}

	return (c);
}
